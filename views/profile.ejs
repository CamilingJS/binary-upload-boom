<%- include('partials/header') -%>
<div class="container">
  <div class="row mt-5 userNameBox-pictureBox-container">
      <div class="col-5 form-username-container">
          <div class="username-box" >
              <div class="username-box-top" >
                <p><strong>User Name</strong>: <%= user.userName %></p>
                <p><strong>Email</strong>: <%= user.email %></p></div>
              <a href="/logout" class="col-3 btn btn-dark">Logout</a>
          </div>
          <div class="mt-5">
            <h2>Add A Post:</h2>
            <form action="/post/createPost" enctype="multipart/form-data" method="POST">
              <div class="mb-3">
                  <label for="title" class="form-label">Title</label>
                  <input type="text" class="form-control" id="title" name="title">
              </div>
              <div class="mb-3">
                <label for="caption" class="form-label">Caption</label>
                <textarea class="form-control" id="caption" name="caption"></textarea>
              </div>
              <div class="mb-3">
                <label for="imgUpload" class="form-label">Image</label>
                <input type="file" class="form-control" id="imageUpload" name="file">
              </div>
              <button type="submit" class="btn btn-dark" value="Upload">Submit</button>
            </form>
          </div>
      </div>
      <div class="col-5 pictureBox">
        <ul class="row justify-content-center">
        <% for(var i=0; i<posts.length; i++) {%>
          <li class="col-6 justify-content-center mt-1">
            <a href="/post/<%= posts[i]._id%>">
              <img class="img-fluid" src="<%= posts[i].image%>">
            </a>
          </li>
        <% } %>
        </ul>
        <div class="row justify-content-center mt-5">
          <a class="btn btn-dark feedBtn" href="/feed">Return to Feed</a>
        </div>  
      </div>  
    </div>
  </div>
</div>
<%- include('partials/footer') -%>